# Changelog

## 0.1.0 — unreleased

- Poor Man's T-SQL formatter through stdin/stdout and a formatting-only LSP.
- Preserve GO repeat directives, quoted text, and batch boundaries.
- Zed adapter with local overrides and pinned npm installation after configuration.
- Settings generator and portable package with offline installation checks.
- Experimental sqlcmd helper with saved connection profiles and terminal output.
- Windows/Linux Node CI, WASM build CI, dependency and secret checks.

Windows, live database, and Zed adapter acceptance remain release gates; see
[release status](docs/release-status.md).
